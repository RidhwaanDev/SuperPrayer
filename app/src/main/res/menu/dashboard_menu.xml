<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <item
        android:id="@+id/search_prayer_menu_item"
        android:title="@string/search_prayer_menu"
        android:icon="@drawable/ic_search_prayer_location"
        app:showAsAction="ifRoom"
        />


    <item
        android:id="@+id/search_prayer_by_date_item"
        android:title="@string/search_prayer_date_menu"
        android:icon="@drawable/ic_month_picker"
        app:showAsAction="ifRoom"
        />

</menu>